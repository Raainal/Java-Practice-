package Workshop;
import java.util.*;
public class math
{
    public static void main()
    {
        double y=4,x=20,z=-3.14;
        System.out.println("Maximum number "+Math.max(x,y));
        System.out.println("Min number "+Math.min(x,y));
        
        System.out.println("sqrt x "+Math.sqrt(x));
        System.out.println("sqrt y "+Math.sqrt(y));
        
        System.out.println("abs number "+Math.abs(z));
        System.out.println("power  number "+Math.pow(x,y));
    }
}
